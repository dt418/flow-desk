{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "packages/db/prisma/schema.prisma", "prisma.config.ts"],
  "globalEnv": ["NODE_ENV", "FLOW_DESK_DB_MODE", "DATABASE_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "^db:generate"],
      "env": ["VITE_API_URL", "VITE_APP_NAME"],
      "outputs": ["dist/**", ".vite/**", "build/**", "generated/**"]
    },
    "dev": {
      "dependsOn": ["^build", "^db:generate"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "db:generate": {
      "cache": false,
      "inputs": ["packages/db/prisma/**/*.prisma"],
      "outputs": ["generated/**"]
    },
    "db:migrate": {
      "cache": false
    },
    "db:migrate-deploy": {
      "cache": false
    },
    "db:seed": {
      "cache": false
    },
    "db:push": {
      "cache": false
    },
    "db:push-skip-generate": {
      "cache": false
    },
    "db:studio": {
      "cache": false,
      "persistent": true
    },
    "db:reset": {
      "cache": false
    }
  }
}
