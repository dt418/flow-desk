# /flowdesk-team

User request: $ARGUMENTS

Load and follow the canonical skill:

`.agents/skills/flowdesk-team/SKILL.md`

## Start at Phase 0 (context). Do not skip security+QA gates when shipping code. For product feature design gates, defer to `plan-feature` when no approved design exists.
